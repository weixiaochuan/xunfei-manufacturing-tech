试用Obsidian总结
可借鉴的地方：界面，功能，思路
改进地方：我们不是要把他作为核心的主界面，所以只是借鉴它的功能和界面设计
主要是有两部分的内容，一个是要有文件处理格式的数据库建立，并且可以在输入的时候，选择是否开启数据库写入功能，如果开启的话，就要调用识别看某句话和某个知识点有多相似，询问是否放进去。但是这个我觉得即时性质的还是不太好用，最好还是使用的过程中像那个JITWORD一样，建立文件之间的关联。然后最后整个文件决定是否要加入我们的个人数据库。
在界面设计上，将个人数据库和文件处理平台作为两个功能点，个人数据文件仿照JITWORD就直接正常编辑即可，可以开启是否启用科研论文编辑功能
我认为现在的界面设计还是不太友好，文字要比意义不明的图片更加方便，第二个就是扩展插件的应用还是太难接入了，可能是因为是英文界面的问题，一定要加入一个英文中文转换功能。
侧边框进入知识库增加部分，这一部分就是决定是否把外部的文件或者是在平台上面那个功能里写好的文件直接按照标准格式写入我们的基础数据库。
具体的技术路线
单独调用
## 一、用户已提到的三个软件及其定位
| 软件 | 核心功能 | 用户在知识库构建中的借鉴方向 |
|------|----------|--------------------------|
| **Obsidian** | 双向链接、Markdown笔记、图谱可视化 | 界面设计、交互方式、知识网络思路 |
| **JitWord** | 高保真文档编辑、Word格式兼容 | 个人数据文件的编辑方式、科研论文编辑功能 |
| **ReliaBuddy** | 质量检验工程文件结构化导入 | 固定结构输入、文件直接结构化导入的思路 |
## 二、对三个软件的借鉴之处
### 1. Obsidian的借鉴
- **界面设计**：笔记编辑器的布局、双向链接的展示方式
- **功能思路**：`[[ ]]` 引用语法（改为 `@平台知识点ID`）、反向链接自动生成
- **知识可视化**：力导向图谱展示知识点关联
### 2. JitWord的借鉴
- **编辑方式**：个人数据文件直接正常编辑
- **功能增强**：可开启/关闭“科研论文编辑功能”
- **格式兼容**：文档解析时保持原有格式（表格、公式、图片位置）
### 3. ReliaBuddy的借鉴
- **导入思路**：把外部文件按标准格式写入基础数据库
- **结构化方式**：固定结构让用户直接输入自己的知识库
- **处理流程**：文档→解析→结构化→用户确认→入库

## 三、知识库构建的核心功能架构

用户设计的功能分为两个主要部分：

### 功能点一：个人数据库
- **输入方式1**：用户在界面手动打字输入（类似Obsidian笔记）
- **输入方式2**：直接传入写好的文件（Word、Excel、PDF）
- **编辑功能**：仿照JitWord正常编辑，可选开启“科研论文编辑功能”

### 功能点二：知识库导入（侧边框进入知识库部分）
- **核心决策**：决定是否把外部文件或平台写好的文件按标准格式写入基础数据库
- **处理能力**：读取Word、Excel、PDF中的文字和图片，识别分析知识点之间的联系
- **输出目标**：转化为与基础知识库格式一致、知识点之间有相互关系的个人知识库

## 四、界面设计思路（最终版）

### 统一的左侧边栏设计
四个导航菜单项：
1. 📝 我的笔记（手动输入知识）
2. 📁 文档导入（批量上传文件）
3. 🕸️ 知识图谱（可视化知识网络）
4. 💬 智能问答（基于统一知识库的问答）

### 三个功能页面对应关系

| 页面 | 功能 | 用户在做什么 |
|------|------|-------------|
| **我的笔记** | 手动输入知识 | 像写笔记一样手动输入工艺经验，使用 `@平台知识点ID` 引用平台基础库 |
| **文档导入** | 批量导入+确认 | 上传Word/Excel/PDF，系统自动解析抽取知识点，用户确认后存入个人库 |
| **知识图谱** | 可视化浏览 | 以图形方式浏览个人知识点与平台知识点的关联网络 |

### 与初版设计的修改之处
- **修正前**：三个界面的侧边栏各自独立
- **修正后**：三个界面使用**完全相同的左侧边栏**，代表同一平台的不同功能页面，仅右侧主内容区切换

## 五、底层统一存储方案

### 核心原则
- 个人知识库和平台基础库不是两个独立的数据库
- 而是**同一个知识图谱**，通过 `source_type` 字段区分来源（platform/user）
- 个人知识点可通过 `platform_ref` 字段关联到平台知识点

### 节点结构要点
```json
{
  "id": "KN_001",
  "source_type": "platform | user",
  "user_id": "user_123 | null",
  "platform_ref": "PROC_002",  // 个人节点关联的平台ID
  ...
}
```

### 个人与平台的联系机制
1. **手动引用**：用户在笔记中使用 `@平台知识点ID` 语法
2. **自动匹配**：导入文档时，系统通过向量相似度推荐关联的平台知识点
3. **反向链接**：被引用的平台节点自动记录哪些个人笔记引用了它
4. **统一检索**：问答时同时检索平台库和个人库，合并结果返回

## 六、关键设计决策汇总

| 决策点 | 结论 |
|--------|------|
| 知识库存储方式 | 统一知识图谱（Neo4j + 向量库），不分开存储 |
| 个人与平台联系 | 通过 `@引用语法` + 自动相似度匹配 + 反向链接 |
| 界面参考 | Obsidian的笔记界面和双向链接交互 |
| 文档处理参考 | ReliaBuddy的结构化导入思路 |
| 编辑功能参考 | JitWord的文档编辑和格式兼容 |
| 侧边栏设计 | 统一侧边栏，不同菜单切换右侧内容 |
| 两种输入方式 | 手动打字输入 + 文件上传导入，并存于同一平台 |

