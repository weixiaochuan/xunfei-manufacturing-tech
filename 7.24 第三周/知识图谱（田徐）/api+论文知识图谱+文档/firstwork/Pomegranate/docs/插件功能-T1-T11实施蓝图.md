# 插件功能 T1-T11 实施蓝图

> **配套文档**：本文档是 [`插件功能.md`](./插件功能.md) §10 设计修订的实施蓝图。
> 阅读顺序：先读 `插件功能.md` §10.1-§10.13 理解设计意图，再用本文件作为编码参照。
>
> **状态**：📋 待实施 | **AppAPI 版本**：1.0.0（Sprint 1 完成后冻结）
> **创建日期**：2026-06-06

---

## 目录

- [1. Sprint 1 执行检查清单](#1-sprint-1-执行检查清单)
- [2. 产出 1：Rust 令牌注册表（T1）](#2-产出-1rust-令牌注册表t1)
- [3. 产出 2：plugin_proxy_* 完整 Command（T2）](#3-产出-2plugin_proxy_-完整-commandt2)
- [4. 产出 3：错误类型与转译表](#4-产出-3错误类型与转译表)
- [5. 产出 4：TypeScript 类型完整草案（T11）](#5-产出-4typescript-类型完整草案t11)
- [6. 产出 5：cc 示例插件 v2 完整草案（T10）](#6-产出-5cc-示例插件-v2-完整草案t10)
- [7. 产出 6：越权攻击测试用例清单（T2 验证）](#7-产出-6越权攻击测试用例清单t2-验证)
- [8. 产出 7：T3 / T4 前端改造蓝图](#8-产出-7t3--t4-前端改造蓝图)
- [9. 配套数据库变更](#9-配套数据库变更)
- [10. 实施风险与回滚预案](#10-实施风险与回滚预案)

---

## 1. Sprint 1 执行检查清单

按顺序执行，每完成一项打勾。

### T11 — TypeScript 类型先行
- [ ] `src/types/index.ts` 追加产出 4 的类型定义
- [ ] `tsc --noEmit` 通过

### T1 — 令牌生命周期
- [ ] 新建 `src-tauri/src/services/plugin_token.rs`（见 §2.1）
- [ ] 新建 `src-tauri/src/commands/plugin_runtime.rs`（见 §2.2）
- [ ] `src-tauri/Cargo.toml` 添加 `uuid = { version = "1", features = ["v4"] }`
- [ ] `state.rs` 加 `plugin_tokens: PluginTokenRegistry` 字段
- [ ] `services/mod.rs` 加 `pub mod plugin_token;`
- [ ] `commands/mod.rs` 加 `pub mod plugin_runtime;`
- [ ] `lib.rs` 的 `generate_handler![]` 注册两个 Command
- [ ] 单元测试通过：acquire 返回 UUID、revoke 幂等、lookup 正确、轮换覆盖

### T2 — plugin_proxy_* 受控 Command
- [ ] 新建 `src-tauri/src/commands/plugin_proxy.rs`（见 §3）
- [ ] `error.rs` 加 `AppError::PluginPermissionDenied`（见 §4）
- [ ] `database/plugin.rs` 加 `has_plugin_permission(plugin_id, perm) -> Result<bool, AppError>` 方法
- [ ] `database/plugin.rs` 加 `get_plugin_setting / set_plugin_setting / get_all_plugin_settings` 方法（如未有）
- [ ] `commands/mod.rs` 加 `pub mod plugin_proxy;`
- [ ] `lib.rs` 注册全部 9 个 plugin_proxy_* Command
- [ ] 跑产出 6 的 12 个测试用例

### T3 — 前端 pluginApi.ts 改造
- [ ] `createAppAPI` 签名加 `token: string` 参数（闭包捕获）
- [ ] 所有 `notes.*` / `settings.*` 调用从 `noteApi.*` 改为 `invoke("plugin_proxy_*", { token, ... })`
- [ ] `hasPermission()` 降级为 UX 前置提示（保留但不再作为安全边界）
- [ ] 新增 `commands` / `sidebar` / `panelViews` / `events` 子 API
- [ ] 加 `version: PLUGIN_API_VERSION` 字段

### T4 — 前端 pluginManager.ts 改造
- [ ] `activatePlugin` 内 `await invoke("plugin_acquire_token", { pluginId })`
- [ ] `deactivatePlugin` 内 `await invoke("plugin_revoke_token", { pluginId })`
- [ ] activate 失败时令牌自动 revoke（错误回滚）
- [ ] 删除 `ctx.registerCommand` / `ctx.registerSidebarItem` / `ctx.registerPanelView` / `ctx.registerSettings` 顶层方法
- [ ] 删除 `window.__pluginCmds_*` / `__pluginSbar_*` / `__pluginViews_*` 全局键
- [ ] PluginManager `extends EventTarget`，替换 `window.dispatchEvent`
- [ ] 注册表 key 强制 `<pluginId>:<id>` 前缀
- [ ] 暴露 `getRegisteredCommands() / getRegisteredSidebarItems() / getRegisteredPanelViews()` 公共方法

### T10 — cc 示例插件适配
- [ ] 重写 `dev-plugins/cc/main.js`（见 §6）
- [ ] 升级 `dev-plugins/cc/plugin.json` 版本号到 2.0.0
- [ ] 验收：cc 插件激活、调用 commands/sidebar/panelViews/notes/settings 全部正常

### Sprint 1 收尾
- [ ] 全部冒烟测试通过
- [ ] 在 `插件功能.md` 标注 "**AppAPI 1.0.0 已冻结**"
- [ ] 提交 commit message：`feat(plugin): 实施 T1-T11 安全模型升级（AppAPI 1.0.0）`

---

## 2. 产出 1：Rust 令牌注册表（T1）

### 2.1 新文件：`src-tauri/src/services/plugin_token.rs`

```rust
//! 插件运行时令牌注册表
//!
//! 设计要点：
//! - 内存存储（Mutex<HashMap>），进程退出自动清空
//! - 每次 acquire 生成新 UUID，覆盖旧令牌（轮换）
//! - 反查表 token → plugin_id 由调用方传 token 时直接遍历查找
//!   （插件总数 < 100，O(n) 性能可接受；若 >1000 再加反向索引）

use std::collections::HashMap;
use std::sync::Mutex;
use uuid::Uuid;

#[derive(Default)]
pub struct PluginTokenRegistry {
    /// pluginId → token
    tokens: Mutex<HashMap<String, String>>,
}

impl PluginTokenRegistry {
    /// 申领新令牌（覆盖旧令牌）
    pub fn acquire(&self, plugin_id: &str) -> Result<String, String> {
        let token = Uuid::new_v4().to_string();
        let mut guard = self.tokens.lock().map_err(|e| e.to_string())?;
        guard.insert(plugin_id.to_string(), token.clone());
        Ok(token)
    }

    /// 作废令牌（幂等）
    pub fn revoke(&self, plugin_id: &str) -> Result<(), String> {
        let mut guard = self.tokens.lock().map_err(|e| e.to_string())?;
        guard.remove(plugin_id);
        Ok(())
    }

    /// 通过令牌反查 plugin_id；找不到 → None
    pub fn lookup(&self, token: &str) -> Result<Option<String>, String> {
        let guard = self.tokens.lock().map_err(|e| e.to_string())?;
        Ok(guard
            .iter()
            .find(|(_, t)| t.as_str() == token)
            .map(|(pid, _)| pid.clone()))
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_acquire_returns_uuid() {
        let registry = PluginTokenRegistry::default();
        let token = registry.acquire("cc").unwrap();
        assert_eq!(token.len(), 36); // UUID v4 长度
        assert!(token.contains('-'));
    }

    #[test]
    fn test_acquire_overwrites_old() {
        let registry = PluginTokenRegistry::default();
        let t1 = registry.acquire("cc").unwrap();
        let t2 = registry.acquire("cc").unwrap();
        assert_ne!(t1, t2);
        // 旧 token 应失效
        assert_eq!(registry.lookup(&t1).unwrap(), None);
        // 新 token 有效
        assert_eq!(registry.lookup(&t2).unwrap(), Some("cc".to_string()));
    }

    #[test]
    fn test_revoke_idempotent() {
        let registry = PluginTokenRegistry::default();
        registry.acquire("cc").unwrap();
        registry.revoke("cc").unwrap();
        registry.revoke("cc").unwrap(); // 第二次不应报错
    }

    #[test]
    fn test_lookup_invalid_token() {
        let registry = PluginTokenRegistry::default();
        assert_eq!(registry.lookup("invalid").unwrap(), None);
    }
}
```

### 2.2 新文件：`src-tauri/src/commands/plugin_runtime.rs`

```rust
//! 插件运行时 Command：令牌生命周期

use crate::state::AppState;
use tauri::State;

/// 申领插件令牌
///
/// 校验：
/// 1. plugin_id 在 plugins 表存在
/// 2. status = "installed"
/// 3. enabled = true
#[tauri::command]
pub fn plugin_acquire_token(
    state: State<'_, AppState>,
    plugin_id: String,
) -> Result<String, String> {
    let plugin = state
        .db
        .get_plugin(&plugin_id)
        .map_err(|e| e.to_string())?
        .ok_or_else(|| format!("插件 {} 不存在", plugin_id))?;

    if plugin.status != "installed" {
        return Err(format!("插件 {} 状态非 installed", plugin_id));
    }
    if !plugin.enabled {
        return Err(format!("插件 {} 已禁用", plugin_id));
    }

    state.plugin_tokens.acquire(&plugin_id)
}

/// 作废插件令牌
#[tauri::command]
pub fn plugin_revoke_token(
    state: State<'_, AppState>,
    plugin_id: String,
) -> Result<(), String> {
    state.plugin_tokens.revoke(&plugin_id)
}
```

### 2.3 AppState 扩展（`state.rs`）

```rust
use crate::services::plugin_token::PluginTokenRegistry;

pub struct AppState {
    pub db: Database,
    pub plugin_tokens: PluginTokenRegistry,  // 新增
    // ... 其他现有字段
}

impl AppState {
    pub fn new(db: Database) -> Self {
        Self {
            db,
            plugin_tokens: PluginTokenRegistry::default(),
            // ...
        }
    }
}
```

### 2.4 Cargo.toml 依赖

```toml
[dependencies]
uuid = { version = "1", features = ["v4"] }
```

---

## 3. 产出 2：plugin_proxy_* 完整 Command（T2）

### 3.1 新文件：`src-tauri/src/commands/plugin_proxy.rs`

#### 3.1.1 权限校验 helper

```rust
//! 插件 proxy Command —— 受控的 IPC 入口
//!
//! 安全模型：
//! 1. 所有调用必须带 token
//! 2. token 反查 plugin_id（无效 → 拒绝）
//! 3. 查询 plugin_permissions 表确认权限（缺失 → 拒绝）
//! 4. 通过 → 委托给对应 Service

use crate::error::AppError;
use crate::state::AppState;
use tauri::State;

/// 统一令牌+权限校验
///
/// 返回值：通过校验的 plugin_id（供后续按插件隔离的操作使用，如 settings）
fn verify(
    state: &AppState,
    token: &str,
    required_permission: Option<&str>,
) -> Result<String, AppError> {
    // 1. 令牌反查
    let plugin_id = state
        .plugin_tokens
        .lookup(token)
        .map_err(AppError::Custom)?
        .ok_or(AppError::PluginPermissionDenied {
            plugin_id: None,
            required_permission: None,
        })?;

    // 2. 权限校验
    if let Some(perm) = required_permission {
        let granted = state
            .db
            .has_plugin_permission(&plugin_id, perm)
            .map_err(|e| AppError::Custom(e.to_string()))?;
        if !granted {
            return Err(AppError::PluginPermissionDenied {
                plugin_id: Some(plugin_id),
                required_permission: Some(perm.to_string()),
            });
        }
    }

    Ok(plugin_id)
}
```

#### 3.1.2 notes 域 6 个 Command

```rust
use crate::models::{Note, NoteCreateInput, NoteUpdateInput, NoteListQuery};
use crate::services::note::NoteService;
use crate::services::search::SearchService;

#[tauri::command]
pub fn plugin_proxy_notes_list(
    state: State<'_, AppState>,
    token: String,
    query: NoteListQuery,
) -> Result<Vec<Note>, String> {
    verify(&state, &token, Some("notes:read")).map_err(|e| e.to_string())?;
    NoteService::list(&state.db, query)
        .map(|page| page.items)
        .map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_notes_get(
    state: State<'_, AppState>,
    token: String,
    id: i64,
) -> Result<Option<Note>, String> {
    verify(&state, &token, Some("notes:read")).map_err(|e| e.to_string())?;
    NoteService::get(&state.db, id).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_notes_search(
    state: State<'_, AppState>,
    token: String,
    keyword: String,
    limit: Option<u32>,
) -> Result<Vec<Note>, String> {
    verify(&state, &token, Some("notes:read")).map_err(|e| e.to_string())?;
    SearchService::search(&state.db, &keyword, limit.unwrap_or(50))
        .map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_notes_create(
    state: State<'_, AppState>,
    token: String,
    input: NoteCreateInput,
) -> Result<Note, String> {
    verify(&state, &token, Some("notes:write")).map_err(|e| e.to_string())?;
    NoteService::create(&state.db, input).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_notes_update(
    state: State<'_, AppState>,
    token: String,
    id: i64,
    input: NoteUpdateInput,
) -> Result<Note, String> {
    verify(&state, &token, Some("notes:write")).map_err(|e| e.to_string())?;
    NoteService::update(&state.db, id, input).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_notes_delete(
    state: State<'_, AppState>,
    token: String,
    id: i64,
) -> Result<(), String> {
    verify(&state, &token, Some("notes:write")).map_err(|e| e.to_string())?;
    NoteService::delete(&state.db, id).map_err(|e| e.to_string())
}
```

#### 3.1.3 settings 域 3 个 Command（插件自身设置无需权限）

```rust
use serde_json::Value;
use std::collections::HashMap;

#[tauri::command]
pub fn plugin_proxy_settings_get(
    state: State<'_, AppState>,
    token: String,
    key: String,
) -> Result<Option<Value>, String> {
    let plugin_id = verify(&state, &token, None).map_err(|e| e.to_string())?;
    state
        .db
        .get_plugin_setting(&plugin_id, &key)
        .map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_settings_set(
    state: State<'_, AppState>,
    token: String,
    key: String,
    value: Value,
) -> Result<(), String> {
    let plugin_id = verify(&state, &token, None).map_err(|e| e.to_string())?;
    state
        .db
        .set_plugin_setting(&plugin_id, &key, &value)
        .map_err(|e| e.to_string())
}

#[tauri::command]
pub fn plugin_proxy_settings_get_all(
    state: State<'_, AppState>,
    token: String,
) -> Result<HashMap<String, Value>, String> {
    let plugin_id = verify(&state, &token, None).map_err(|e| e.to_string())?;
    state
        .db
        .get_all_plugin_settings(&plugin_id)
        .map_err(|e| e.to_string())
}
```

### 3.2 lib.rs 注册增量

```rust
.invoke_handler(tauri::generate_handler![
    // ... 现有 Commands ...

    // ===== T1 令牌管理 =====
    commands::plugin_runtime::plugin_acquire_token,
    commands::plugin_runtime::plugin_revoke_token,

    // ===== T2 proxy commands =====
    commands::plugin_proxy::plugin_proxy_notes_list,
    commands::plugin_proxy::plugin_proxy_notes_get,
    commands::plugin_proxy::plugin_proxy_notes_search,
    commands::plugin_proxy::plugin_proxy_notes_create,
    commands::plugin_proxy::plugin_proxy_notes_update,
    commands::plugin_proxy::plugin_proxy_notes_delete,
    commands::plugin_proxy::plugin_proxy_settings_get,
    commands::plugin_proxy::plugin_proxy_settings_set,
    commands::plugin_proxy::plugin_proxy_settings_get_all,
])
```

---

## 4. 产出 3：错误类型与转译表

### 4.1 error.rs 增量

```rust
#[derive(Debug, thiserror::Error)]
pub enum AppError {
    // ... 现有变体 ...

    #[error("插件权限拒绝: plugin={plugin_id:?} required={required_permission:?}")]
    PluginPermissionDenied {
        plugin_id: Option<String>,
        required_permission: Option<String>,
    },
}
```

### 4.2 前端错误消息转译表

| Rust 错误原文模式 | 触发条件 | 前端展示 |
|---|---|---|
| `插件权限拒绝: plugin=None required=None` | 令牌无效/已撤销 | `插件会话已过期，请重新启用插件` |
| `插件权限拒绝: plugin=Some("xxx") required=Some("notes:write")` | 令牌有效但缺权限 | `插件 xxx 缺少权限：notes:write` |
| `插件 X 不存在` | acquire_token 时插件已被卸载 | `插件已卸载，请刷新页面` |
| `插件 X 已禁用` | acquire_token 时插件被禁用 | `插件已禁用` |
| `插件 X 状态非 installed` | acquire_token 时插件正在安装/卸载 | `插件状态异常，请稍后重试` |

转译实现位置：`pluginApi.ts` 加 `translateError(rawError: string): string` 函数。

---

## 5. 产出 4：TypeScript 类型完整草案（T11）

追加到 `src/types/index.ts`：

```typescript
// ========== 插件 API v1.0.0 ==========

/** AppAPI 版本号（与 Rust 侧固定字符串保持一致） */
export const PLUGIN_API_VERSION = "1.0.0" as const;

export interface PluginCommandDef {
  /** 命令 ID（不含 pluginId 前缀，PluginManager 会自动加） */
  id: string;
  name: string;
  group?: string;
  hotkey?: string;
  callback: () => void | Promise<void>;
}

export interface PluginSidebarItemDef {
  id: string;
  icon: string;           // Lucide 图标名
  label: string;
  viewId?: string;
  onClick?: () => void;
}

export interface PluginPanelViewDef {
  id: string;
  title: string;
  /** 渲染函数：接收容器 DOM，返回 cleanup */
  render: (container: HTMLElement) => void | (() => void);
  /** 由 PluginManager 填充，插件不应设置 */
  pluginId?: string;
}

// ---- 子 API ----

export interface PluginCommandsAPI {
  addCommand(def: PluginCommandDef): void;
  removeCommand(id: string): void;
  /** 执行任意已注册命令（含其他插件，需带完整 pluginId 前缀） */
  executeCommand(fullId: string): void | Promise<void>;
}

export interface PluginSidebarAPI {
  addItem(def: PluginSidebarItemDef): void;
  removeItem(id: string): void;
}

export interface PluginPanelViewsAPI {
  register(def: PluginPanelViewDef): void;
  unregister(id: string): void;
}

export interface PluginNotesAPI {
  list(query?: NoteListQuery): Promise<Note[]>;
  get(id: number): Promise<Note | null>;
  search(keyword: string, limit?: number): Promise<Note[]>;
  create(input: NoteCreateInput): Promise<Note>;
  update(id: number, input: NoteUpdateInput): Promise<Note>;
  delete(id: number): Promise<void>;
}

export interface PluginSettingsAPI {
  get<T = unknown>(key: string): Promise<T | undefined>;
  set(key: string, value: unknown): Promise<void>;
  getAll(): Promise<Record<string, unknown>>;
}

export interface PluginWorkspaceAPI {
  getActiveNoteId(): number | null;
  /** 需 notes:read */
  getActiveNote(): Promise<Note | null>;
}

export interface PluginNoticesAPI {
  show(message: string, duration?: number): void;
  error(message: string): void;
}

/** 事件总线（R6，类型先冻结，运行时可阶段 2 末再实现） */
export interface PluginEvents {
  "workspace:active-note-changed": { noteId: number | null };
  "workspace:editor-saved": { noteId: number };
  "settings:changed": { key: string };
}

export interface PluginEventBus {
  on<K extends keyof PluginEvents>(
    event: K,
    handler: (data: PluginEvents[K]) => void,
  ): () => void;
  emit<K extends keyof PluginEvents>(event: K, data: PluginEvents[K]): void;
}

// ---- 顶层 ----

export interface PluginAppAPI {
  readonly version: typeof PLUGIN_API_VERSION;
  workspace: PluginWorkspaceAPI;
  notes: PluginNotesAPI;
  settings: PluginSettingsAPI;
  notices: PluginNoticesAPI;
  commands: PluginCommandsAPI;
  sidebar: PluginSidebarAPI;
  panelViews: PluginPanelViewsAPI;
  events: PluginEventBus;
}

export interface PluginContext {
  app: PluginAppAPI;
  meta: Readonly<PluginMeta>;
  // 注意：v1.0 已移除 registerCommand / registerSidebarItem / registerPanelView / registerSettings
}
```

---

## 6. 产出 5：cc 示例插件 v2 完整草案（T10）

### 6.1 `dev-plugins/cc/main.js`（破坏式重写）

```js
/**
 * cc 示例插件 v2 —— 适配 AppAPI 1.0.0
 *
 * 演示新 API 的全部 5 个域：
 *   - commands  注册命令面板项
 *   - sidebar   注册侧边栏图标
 *   - panelViews 注册面板视图
 *   - notes     调用受控笔记 API
 *   - settings  读写插件自身设置
 */

module.exports = {
  async onLoad(ctx) {
    const { app, meta } = ctx;

    console.log(`[cc] 激活中... API version=${app.version}, plugin=${meta.id}`);

    // ===== 1. 注册命令 =====
    app.commands.addCommand({
      id: "say-hello",
      name: "CC: 打招呼",
      group: "示例",
      callback: () => {
        app.notices.show(`Hello from ${meta.name}!`);
      },
    });

    app.commands.addCommand({
      id: "count-notes",
      name: "CC: 统计笔记数量",
      group: "示例",
      callback: async () => {
        try {
          const notes = await app.notes.list();
          app.notices.show(`当前共有 ${notes.length} 篇笔记`);
        } catch (e) {
          app.notices.error(`查询失败: ${e}`);
        }
      },
    });

    // ===== 2. 注册侧边栏图标 =====
    app.sidebar.addItem({
      id: "cc-panel",
      icon: "Sparkles",
      label: "CC 示例",
      viewId: "cc-main-view",
    });

    // ===== 3. 注册面板视图 =====
    app.panelViews.register({
      id: "cc-main-view",
      title: "CC 控制面板",
      render: (container) => {
        container.innerHTML = `
          <div style="padding: 16px">
            <h3>CC 示例插件</h3>
            <p>这是一个演示 AppAPI 1.0.0 的示例插件</p>
            <button id="cc-btn-stat">统计笔记</button>
            <pre id="cc-output" style="margin-top: 16px; background: #f0f0f0; padding: 8px"></pre>
          </div>
        `;
        const btn = container.querySelector("#cc-btn-stat");
        const out = container.querySelector("#cc-output");
        const handler = async () => {
          try {
            const notes = await app.notes.list();
            out.textContent = `笔记数量: ${notes.length}\n最近一篇: ${notes[0]?.title ?? "无"}`;
          } catch (e) {
            out.textContent = `错误: ${e}`;
          }
        };
        btn.addEventListener("click", handler);

        // 返回 cleanup
        return () => {
          btn.removeEventListener("click", handler);
        };
      },
    });

    // ===== 4. 读写插件自身设置 =====
    const lastRun = await app.settings.get("lastRun");
    console.log(`[cc] 上次激活: ${lastRun ?? "首次"}`);
    await app.settings.set("lastRun", new Date().toISOString());

    // ===== 5. 监听事件（运行时可能尚未实现，做存在性检查）=====
    if (app.events && typeof app.events.on === "function") {
      const off = app.events.on("workspace:active-note-changed", ({ noteId }) => {
        console.log(`[cc] 当前笔记切换到: ${noteId}`);
      });
      ctx._ccUnsubscribe = off;
    }

    console.log(`[cc] 激活完成`);
  },

  async onUnload(ctx) {
    console.log(`[cc] 停用中...`);
    if (ctx._ccUnsubscribe) ctx._ccUnsubscribe();
    // commands / sidebar / panelViews 由 PluginManager 自动按 pluginId 前缀清理
  },
};
```

### 6.2 `dev-plugins/cc/plugin.json`

```json
{
  "id": "cc",
  "name": "CC 示例插件",
  "version": "2.0.0",
  "main": "main.js",
  "permissions": ["notes:read"],
  "minAppVersion": "1.0.0"
}
```

---

## 7. 产出 6：越权攻击测试用例清单（T2 验证）

放置于 `src-tauri/tests/plugin_proxy_test.rs`（新建集成测试文件）。

| # | 场景 | 输入 | 期望结果 |
|---|---|---|---|
| 1 | 无效令牌 | `token="random-junk"` | `Err("插件权限拒绝: plugin=None required=None")` |
| 2 | 空令牌 | `token=""` | 同 #1 |
| 3 | 已撤销令牌 | acquire 后 revoke，再用旧 token 调 list | 同 #1 |
| 4 | 已轮换令牌 | acquire 两次，用第一次的 token | 同 #1（被覆盖） |
| 5 | 有效令牌+缺 notes:read | cc 未授权 notes:read，调 list | `Err("...plugin=Some(\"cc\") required=Some(\"notes:read\")")` |
| 6 | 有效令牌+缺 notes:write | 仅授 notes:read，调 create | `Err("...required=Some(\"notes:write\")")` |
| 7 | 有效令牌+完整权限 | 授 notes:read，调 list | `Ok(Vec<Note>)` |
| 8 | 跨插件令牌互窜 | A 插件 token 调 settings_get_all | 应只返回**令牌所属插件**的设置（A 的，不是 B 的） |
| 9 | 卸载后令牌仍调用 | acquire token → 卸载插件（不 revoke）→ 调 list | revoke 应在卸载流程内置；如漏掉则会成功，**这是 PluginManager 必须做的兜底** |
| 10 | 令牌泄漏到 console | 检查 token 是否出现在 console.log/错误堆栈 | 不应出现 |
| 11 | 并发申领 | 两线程同时 acquire 同一 plugin_id | 后到者覆盖，前者 token 失效 |
| 12 | 大量插件性能 | 注册 1000 个插件，lookup 耗时 | < 1ms（O(n) 可接受） |

---

## 8. 产出 7：T3 / T4 前端改造蓝图

### 8.1 新版 `createAppAPI` 签名（T3）

```typescript
export function createAppAPI(
  pluginId: string,
  grantedPermissions: string[],
  pluginDir: string,
  token: string,  // ← T3 新增：闭包捕获令牌
  manager: PluginManager,  // ← T4 新增：注册类 API 委托给 manager 的内部 Map
): PluginAppAPI {
  const granted = new Set(grantedPermissions);

  // 前置 UX 检查（仅提示，非安全边界）
  function preCheck(perm: string) {
    if (!granted.has(perm)) {
      console.warn(`[plugin:${pluginId}] 调用需要权限 ${perm}，但插件未声明`);
    }
  }

  return {
    version: PLUGIN_API_VERSION,

    notes: {
      list: async (query) => {
        preCheck("notes:read");
        return invoke("plugin_proxy_notes_list", { token, query: query ?? {} });
      },
      get: async (id) => {
        preCheck("notes:read");
        return invoke("plugin_proxy_notes_get", { token, id });
      },
      search: async (keyword, limit) => {
        preCheck("notes:read");
        return invoke("plugin_proxy_notes_search", { token, keyword, limit });
      },
      create: async (input) => {
        preCheck("notes:write");
        return invoke("plugin_proxy_notes_create", { token, input });
      },
      update: async (id, input) => {
        preCheck("notes:write");
        return invoke("plugin_proxy_notes_update", { token, id, input });
      },
      delete: async (id) => {
        preCheck("notes:write");
        return invoke("plugin_proxy_notes_delete", { token, id });
      },
    },

    settings: {
      get: async (key) => invoke("plugin_proxy_settings_get", { token, key }),
      set: async (key, value) => invoke("plugin_proxy_settings_set", { token, key, value }),
      getAll: async () => invoke("plugin_proxy_settings_get_all", { token }),
    },

    commands: {
      addCommand: (def) => manager._addCommand(pluginId, def),
      removeCommand: (id) => manager._removeCommand(pluginId, id),
      executeCommand: (fullId) => manager._executeCommand(fullId),
    },

    sidebar: {
      addItem: (def) => manager._addSidebarItem(pluginId, def),
      removeItem: (id) => manager._removeSidebarItem(pluginId, id),
    },

    panelViews: {
      register: (def) => manager._registerPanelView(pluginId, def),
      unregister: (id) => manager._unregisterPanelView(pluginId, id),
    },

    notices: {
      show: (msg, duration) => message.success(msg, duration ?? 3),
      error: (msg) => message.error(msg),
    },

    workspace: {
      getActiveNoteId: () => {
        const match = window.location.hash.match(/\/notes\/(\d+)/);
        return match ? Number(match[1]) : null;
      },
      getActiveNote: async () => {
        preCheck("notes:read");
        const id = (() => {
          const m = window.location.hash.match(/\/notes\/(\d+)/);
          return m ? Number(m[1]) : null;
        })();
        if (id == null) return null;
        return invoke("plugin_proxy_notes_get", { token, id });
      },
    },

    events: createEventBus(pluginId, manager),  // 见 R6 实现
  };
}
```

### 8.2 PluginManager 内部注册表（T4）

```typescript
class PluginManager extends EventTarget {
  // 注册表（key = "<pluginId>:<id>"）
  private registry = {
    commands:   new Map<string, PluginCommandDef & { pluginId: string }>(),
    sidebar:    new Map<string, PluginSidebarItemDef & { pluginId: string }>(),
    panelViews: new Map<string, PluginPanelViewDef>(),
  };

  // ---- 给 createAppAPI 的内部方法（下划线前缀提示"非公开"） ----

  _addCommand(pluginId: string, def: PluginCommandDef) {
    const key = `${pluginId}:${def.id}`;
    this.registry.commands.set(key, { ...def, pluginId });
    this.dispatchEvent(new Event("registry:commands-changed"));
  }

  _removeCommand(pluginId: string, id: string) {
    this.registry.commands.delete(`${pluginId}:${id}`);
    this.dispatchEvent(new Event("registry:commands-changed"));
  }

  _executeCommand(fullId: string) {
    const cmd = this.registry.commands.get(fullId);
    if (!cmd) throw new Error(`命令 ${fullId} 未注册`);
    return cmd.callback();
  }

  _addSidebarItem(pluginId: string, def: PluginSidebarItemDef) {
    const key = `${pluginId}:${def.id}`;
    this.registry.sidebar.set(key, { ...def, pluginId });
    this.dispatchEvent(new Event("registry:sidebar-changed"));
  }

  _removeSidebarItem(pluginId: string, id: string) {
    this.registry.sidebar.delete(`${pluginId}:${id}`);
    this.dispatchEvent(new Event("registry:sidebar-changed"));
  }

  _registerPanelView(pluginId: string, def: PluginPanelViewDef) {
    const key = `${pluginId}:${def.id}`;
    this.registry.panelViews.set(key, { ...def, pluginId });
    this.dispatchEvent(new Event("registry:views-changed"));
  }

  _unregisterPanelView(pluginId: string, id: string) {
    this.registry.panelViews.delete(`${pluginId}:${id}`);
    this.dispatchEvent(new Event("registry:views-changed"));
  }

  // ---- 公共查询方法（供 React 组件订阅） ----

  getRegisteredCommands(): PluginCommandDef[] {
    return Array.from(this.registry.commands.values());
  }

  getRegisteredSidebarItems(): PluginSidebarItemDef[] {
    return Array.from(this.registry.sidebar.values());
  }

  getRegisteredPanelViews(): PluginPanelViewDef[] {
    return Array.from(this.registry.panelViews.values());
  }

  // ---- 插件卸载时清理（按前缀批量删） ----

  private cleanupPluginRegistry(pluginId: string) {
    const prefix = `${pluginId}:`;
    for (const key of [...this.registry.commands.keys()]) {
      if (key.startsWith(prefix)) this.registry.commands.delete(key);
    }
    for (const key of [...this.registry.sidebar.keys()]) {
      if (key.startsWith(prefix)) this.registry.sidebar.delete(key);
    }
    for (const key of [...this.registry.panelViews.keys()]) {
      if (key.startsWith(prefix)) this.registry.panelViews.delete(key);
    }
    this.dispatchEvent(new Event("registry:commands-changed"));
    this.dispatchEvent(new Event("registry:sidebar-changed"));
    this.dispatchEvent(new Event("registry:views-changed"));
  }
}
```

### 8.3 React 组件订阅 Hook（参考实现）

```typescript
// src/hooks/usePluginRegistry.ts
import { useEffect, useState } from "react";
import { pluginManager } from "@/services/pluginManager";
import type { PluginCommandDef } from "@/types";

export function usePluginCommands(): PluginCommandDef[] {
  const [items, setItems] = useState(() => pluginManager.getRegisteredCommands());
  useEffect(() => {
    const handler = () => setItems(pluginManager.getRegisteredCommands());
    pluginManager.addEventListener("registry:commands-changed", handler);
    return () => pluginManager.removeEventListener("registry:commands-changed", handler);
  }, []);
  return items;
}
```

---

## 9. 配套数据库变更

### 9.1 现有表无 Schema 变更

T1 令牌注册表在 Rust 内存中，**不入 SQLite**。
`plugin_permissions` / `plugin_settings` 表假定已存在（由阶段 1 创建），如未创建需补迁移。

### 9.2 Database 层新增方法清单

需在 `src-tauri/src/database/plugin.rs` 添加：

| 方法签名 | 用途 |
|---|---|
| `has_plugin_permission(plugin_id: &str, perm: &str) -> Result<bool, AppError>` | 查 plugin_permissions 表是否存在指定记录 |
| `get_plugin_setting(plugin_id: &str, key: &str) -> Result<Option<Value>, AppError>` | 查询单个设置 |
| `set_plugin_setting(plugin_id: &str, key: &str, value: &Value) -> Result<(), AppError>` | Upsert 单个设置 |
| `get_all_plugin_settings(plugin_id: &str) -> Result<HashMap<String, Value>, AppError>` | 列出某插件全部设置 |

如这些方法已部分存在（`pluginApi.ts` 中 `backendPluginApi.getSettings` 表明 `get_all` 已有），补齐缺失的即可。

---

## 10. 实施风险与回滚预案

### 10.1 风险

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 现有 cc 插件中断 | 高 | 中 | T10 同步升级；项目无对外插件生态，影响可控 |
| Rust 端权限表方法未实现 | 中 | 高 | §9.2 提前检查，缺则补 |
| 阶段 1 已隐式开展的 UI 注入代码引用旧 API | 高 | 中 | T4 完成后跑 `tsc --noEmit` 一次性暴露 |
| 令牌泄漏到日志 | 低 | 高 | 测试用例 #10 强制覆盖 |
| Database 层无 has_plugin_permission 方法 | 高 | 高 | T2 实施前先补 §9.2 方法 |

### 10.2 回滚预案

Sprint 1 用单一 commit 提交，便于 `git revert`：
```bash
git revert HEAD  # 一键回滚 T1-T11
```

回滚后旧 API 立即恢复，cc 插件可用回 v1。

### 10.3 渐进式验证

| 步骤 | 验证方式 |
|---|---|
| T1 完成 | `cargo test plugin_token` 通过 |
| T2 完成 | 跑 §7 的 12 个测试用例 |
| T3 完成 | cc 插件能 `await ctx.app.notes.list()` |
| T4 完成 | DevTools 中 `window.__plugin*` 为 undefined |
| T10 完成 | cc 插件激活后命令面板能搜到 `CC: 打招呼` |
| T11 完成 | `tsc --noEmit` 0 errors |

---

**文档结束** | 实施完成后请在 `插件功能.md` 的更新日志中追加一条记录。
