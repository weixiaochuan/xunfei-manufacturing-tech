# 本会话开发内容总览

## 1. 当前状态

`settings/index.tsx` 已从 KnowledgeBase 源 (`D:/AI/ALL-Note/KnowledgeBase/srcGitee`) 恢复（2570 行），但存在兼容性错误，需要适配才能编译通过。

以下为本会话完成的**全部改动**，按时间顺序记录，供恢复参考。

---

## 2. 已完成的文件改动

### 2.1 新建文件

| 文件 | 用途 |
|------|------|
| `src/components/about/AboutContent.tsx` | 可复用关于内容组件（从 `/about` 页抽取），支持 `showHeader`/`showSponsor`/`showRecommend`/`idPrefix` props |

### 2.2 修改文件

#### `src/store/index.ts` — 扩展状态属性

**新增类型/常量**：
- `EditorRuleLines` 类型 ("none" | "lines" | "grid")
- `EDITOR_READING_WIDTH_OPTIONS` / `EDITOR_READING_WIDTH_LABELS`
- `EDITOR_RULE_LABELS`
- `EDITOR_LAYOUT_DEFAULTS`

**新增接口属性**：
```
editorReadingWidth, editorPaper, editorRuleLines, editorFirstLineIndent
themeOverridesEnabled, customAccent, customBgImage, customBgDim, customBgBlur, customBgFit
```

**新增 setter 方法**：
```
setEditorReadingWidth, setEditorPaper, setEditorRuleLines, setEditorFirstLineIndent
setThemeOverridesEnabled, setCustomAccent, setCustomBgImage, setCustomBgDim, setCustomBgBlur, setCustomBgFit
resetThemeOverrides
```

**持久化**：全部新属性已加入 `loadFromStore` / `saveThemeToStore` / subscribe key。

#### `src/pages/about/index.tsx` — 简化为使用 AboutContent

#### `src/components/settings/FeatureModulesSection.tsx` — 增加可选 `title`/`description` props

#### `src/components/ui/RecommendCards.tsx` — 清空为空组件（返回 null）

#### `src/components/about/AboutContent.tsx` — 作者社区信息更新

```
移除: B站主页、视频讲解、知识星球入口
更新: QQ群号 → 897442341, 联系作者 QQ → 3956643, 微信 → Wen_Jing_Qian
```

#### `src/styles/global.css` — 新增设置页 Tabs 样式

```css
.settings-tabs-page { ... }
.settings-tabs-shell { ... }
.settings-tabs-stack { ... }
```

---

## 3. 设置页 Tab 重组方案

### 3.1 目标 Tab 结构（7 个顶级 Tab）

| # | Tab 标签 | 图标 | 内容 |
|---|---------|------|------|
| 1 | **AI能力中心** | Bot | AI模型卡片 + AI Provider注册表 + 语音识别 |
| 2 | **应用管理** | AppWindow | AI应用卡片 + Claude Agent Runner + MCP服务器 |
| 3 | **应用配置** | Cog | 数据目录 + 日记路径 + 孤儿素材 + 自动保存 + 导入导出 + 同步备份 + 插件设置 |
| 4 | **个性设置** | UserRound | 待办提醒 + 笔记模板 + 隐藏PIN + 快捷键 |
| 5 | **显示设置** | Monitor | 外观自定义 + 编辑器外观 |
| 6 | **更新启动** | Rocket | 软件更新 + 启动设置 + 侧边栏功能模块 |
| 7 | **关于** | Info | 系统信息 + 数据迁移说明 + 作者社区 |

### 3.2 关键 UI 变更

1. **锚点导航 → 竖直 Tabs**：用 `<Tabs tabPosition="left">` 替代 `SettingsAnchorNav` + 滚动布局
2. **`scrollTo` 兼容**：旧锚点 id 映射到新 Tab key（`sectionIdToTab` 函数）
3. **Tab 从不销毁**：`destroyInactiveTabPane={false}` 保持状态
4. **弹窗保持顶层**：导入预览/模型编辑/模板编辑/更新弹窗不下沉到 Tab 内部
5. **Tab 图标**：全部 7 个 Tab 使用 lucide-react 图标
6. **移除顶部副标题**："应用配置与 AI 模型管理"已删除
7. **移除 AI 介绍信息框**："管理聊天模型、AI 应用、MCP 工具与 Claude Code 执行能力"已删除
8. **应用管理提升**：从 AI能力中心子Tab 提升为独立顶级Tab

### 3.3 显示设置 Tab — 外观自定义完整迁移

来自 KnowledgeBase 的完整外观配置：
- 启用自定义 → Switch 总开关
- 强调色 → ColorPicker + 8色快速色板
- 背景图 → 选择/更换/清除（Phase 1 后端占位）
- 背景遮罩 → Slider 0-100%
- 背景适配 → Select 覆盖/包含/原始/平铺
- 背景模糊 → Slider 0-30px

编辑器外观扩展：
- 新增：阅读列宽 Select
- 新增：纸张观感 Switch
- 新增：背景纹理 Radio (无/横线/网格)
- 新增：首行缩进 Switch
- 新增：高亮快捷键 Tag 展示

---

## 4. 已知问题

1. **`src/pages/settings/index.tsx`**：当前从 KnowledgeBase 复制而来，存在兼容性错误（缺少 `UI_SCALE_OPTIONS`、`suggestUiScale`、`autoHideActivityBar`、`EditorHighlightShortcutRow`、`UpdaterProvider`、`uiScale`/`autoSaveEnabled` 等 store 属性），需要适配
2. **Store**：新加的 `editorReadingWidth`/`editorPaper`/`editorRuleLines`/`editorFirstLineIndent`/`themeOverrides*`/`customAccent*`/`customBg*` 属性已持久化但编辑器/主题系统尚未消费这些值
3. **后端 `copy_theme_bg`/`clear_theme_bg`**：Phase 1 占位，前端 try-catch 兜底

---

## 5. 恢复步骤建议

### 优先恢复（核心功能）

```bash
# 1. 修复 settings/index.tsx TypeScript 错误
cd D:/AI/W-NoteBook && npx tsc --noEmit

# 2. 逐个解决缺失依赖：
#    - 添加 UI_SCALE_OPTIONS / suggestUiScale 到 store（或从 settings 移除引用）
#    - 添加 autoHideActivityBar / notesCollapseFoldersOnStartup / autoSaveEnabled 等到 store
#    - 创建或移除 EditorHighlightShortcutRow / UpdaterProvider 引用
```

### 已持久化的 store 新属性（不可丢失）

```
editorReadingWidth, editorPaper, editorRuleLines, editorFirstLineIndent,
themeOverridesEnabled, customAccent, customBgImage, customBgDim, customBgBlur, customBgFit
```
